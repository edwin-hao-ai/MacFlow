# Icons

This directory should contain:
- `icon.icns` — macOS app icon (Tauri will use this)

Generate from a 1024x1024 PNG with:
```
bun tauri icon path/to/source.png
```

For now (dev), a placeholder `icon.icns` is generated by running `bun tauri icon`.
